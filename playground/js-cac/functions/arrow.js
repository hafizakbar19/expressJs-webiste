// // let addTow = (num1, num2) => (num1 + num2);
// let addTow = (num1, num2) => ({user: 'Akbar'});

// console.log(addTow());


//IIFE  immediately invoked function expression


( (name)=>{
    console.log(`hello ${name}`);
} )('Akbar');

( (name)=>{
    console.log(`hello ${name}`);
} )('Ali');

